-- run using <script-name.sql> | mysql -hlocalhost -uroot -p <database-name>
-- shows tables in a database passed as an argument from tty
CREATE TABLE IF NOT EXISTS `first_table` (`id` INT, `name` VARCHAR(256));
