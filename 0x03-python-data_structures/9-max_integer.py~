#!/usr/bin/python3
def max_integer(my_list=[]):
    a = 0
    for i in range(len(my_list) - 1):
        if my_list[i+1] < my_list[i]:
            a = my_list[i]
        else:
            a = my_list[i+1]
    return a
